class Stack {
  constructor() {
    this.stackControl = [];
    this.MAX_SIZE = 10;
  }

  canPush() {
    return (this.stackControl < this.MAX_SIZE)




    // if ( < 10) {
    //   return true
    // } if else (Stack > 0){
    //   return true
    // } else
    // ... your code goes here 
    //!!20.31!!
  }

  isEmpty() {
    if (this.stackControl = 0 || this.stackControl != 0) {
      return this.stackControl
    }

    //return (this.stackControl = 0 === true || this.stackControl != 0 != true)
    //return this.stackControl.length === 0 hours later
    //primer día que uso === !=

    // ... your code goes here
  }

  push(item) {

    if
    //return (this.canPush === this.stackControl.push(item))

    // if (this.stackControl < this.MAX_SIZE) {
    //   this.stackControl.push(item)
    // } return this.stackControl.length // PORQUE NO TIENE SENTIDOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOO should return the stack when an element is inserted


    //
    // HE INTENTADO QUE DEVOLVIERA LA LONGITUD DE this.MAX_SIZE PERO HA SIDO EN VANO .length not works // mucho




    // ... your code goes here
  }
  pop() {
    // ... your code goes here
  }

  display() {
    // ... your code goes here
  }
}

// This is required to enable the automated tests, please ignore it.
if (typeof module !== 'undefined') module.exports = Stack;
