let addButton = document.getElementsByClassName("add-btn")[0]
let takeButton = document.getElementsByClassName("take-btn")[0]
let input = document.getElementById("inputId")

let newStack = new StackDataStructure

function addContent(elm) {
    if(newStack.canPush()){
        let
    }
}