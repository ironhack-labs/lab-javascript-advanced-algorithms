

const inputText = document.getElementById('input');
const blocks = document.querySelectorAll('#div-bars .bars');
const addValue = document.getElementById('btn-blue');
const reduceValue = document.getElementById('btn-red');


// pres blue btn and take the value of input to the stack 
// select the div and change the innerText

function selectableDiv() {
    addValue
}



// check which div is possible to add the entered text
// selesct the div
// change the innerText of the selected div to enter the input 
// 


const stack = new StackDataStructure();


addValue.onclick = selectableDiv;
reduceValue.onclick = reduceableDiv;

